# PostgreSQL Configuration (Optional)
# Set USE_POSTGRES to True to use PostgreSQL instead of SQLite
USE_POSTGRES=False

# PostgreSQL Database Settings (only used if USE_POSTGRES=True)
DB_NAME=suivi_collecte
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
